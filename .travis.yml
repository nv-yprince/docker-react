language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t yprince/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true yprince/docker-react npm run test -- --coverage